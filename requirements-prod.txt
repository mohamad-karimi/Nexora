# Production runtime dependencies only.
#
# This is the SAME pinned versions as requirements.txt, minus dev/test/lint
# tooling (black, flake8, pytest*, coverage) that has no reason to ship in
# the production image. Used only by dockerfile.prod
# (docker-compose.prod.yml); requirements.txt (used by dockerfile / the
# existing dev docker-compose.yml / CI) is untouched.
#
# Keep this list in sync with requirements.txt whenever a runtime
# dependency's version changes.

# Django
Django==5.2.14
djangorestframework==3.16.1
django-filter==26.1

# Image Processing
Pillow==12.3.0

# Configuration
python-decouple==3.8

# Redis client (used by core/redis_client.py)
redis==8.1.0

# Background jobs (core/celery.py; broker = the Redis above).
celery==5.6.3

# JWT
PyJWT==2.9.0

# API Documentation
drf-spectacular==0.30.0

# jwt
djangorestframework-simplejwt==5.5.0

# Production WSGI server
gunicorn==23.0.0

# PostgreSQL client
psycopg2-binary==2.9.10
