# Django
Django==5.2.14
djangorestframework==3.16.1
django-filter==26.1

# Image Processing
Pillow==12.3.0

# Configuration
python-decouple==3.8

# Redis client (used by core/redis_client.py)
redis==8.1.0

# Background jobs (core/celery.py; broker = the Redis above).
# Deliberately NOT "celery[redis]": that extra caps redis<6.5 and would
# downgrade the redis==8.1.0 pinned above. kombu's Redis transport runs on
# it fine (task round trip verified against a real Redis).
celery==5.6.3

# JWT
PyJWT==2.9.0

# Code Quality / Formatting
black
flake8==6.1.0

# API Documentation
drf-spectacular==0.30.0

# jwt
djangorestframework-simplejwt==5.5.0

# Testing (used by pytest.ini / conftest.py, required for CI)
pytest==8.3.4
pytest-django==4.9.0
pytest-cov==6.0.0
coverage==7.6.9

# Production WSGI server (used by docker-compose.prod.yml)
gunicorn==23.0.0

# PostgreSQL client (used when POSTGRES_DB is set -- see core/core/settings.py
# and docker-compose.prod.yml). Kept in the main requirements file too so
# the existing dev/CI image and test suite can run against Postgres if
# needed; requirements-prod.txt (production image) also pins it.
psycopg2-binary==2.9.10

# Serves static files directly from the WSGI app (core/core/settings.py
# MIDDLEWARE / STORAGES). Needed on platforms with no separate static-file
# server in front of Gunicorn (e.g. Render) -- see docs/production-
# deployment.md. Harmless on the existing self-hosted stack: there, Nginx
# already serves /static/ from a volume before a request ever reaches
# Django, so this middleware never actually gets exercised.
whitenoise==6.12.0
